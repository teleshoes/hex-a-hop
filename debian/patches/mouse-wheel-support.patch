Index: hex-a-hop.svn/menus.h
===================================================================
--- hex-a-hop.svn.orig/menus.h	2007-09-20 00:49:31.000000000 +0200
+++ hex-a-hop.svn/menus.h	2007-09-20 00:49:41.000000000 +0200
@@ -312,6 +312,10 @@
 			else
 				Cancel();
 		}
+		else if (buttons_pressed==8)
+			Move(-1);
+		else if (buttons_pressed==16)
+			Move(1);
 		else
 			Menu::Mouse(x,y,dx,dy,buttons_pressed, buttons_released, buttons);	
 	}
